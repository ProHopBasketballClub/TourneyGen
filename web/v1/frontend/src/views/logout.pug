html(lang='en')
if current_user
    body 
        div(class = 'logout')

            p
                | You are currently logged in as #{current_user.displayName}
            form(action = '/logout' method='POST')
                input(type="submit" id=""  value="Logout")
