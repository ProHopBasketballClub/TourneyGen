/*
import app from './App'

const port = process.env.PORT || 3000

app.listen(port, (err) => {
    if (err) {
        return console.log(err)
    }

    return console.log(`server is listening on ${port}`)
})
*/
document.body.innerHTML = "<p>We've got a deploy goin' on the front-end></p>";
